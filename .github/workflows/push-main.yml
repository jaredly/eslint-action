# AUTOGENERATED by workflow-preprocessor.js from ../.github/workflow-templates/push-main.yml

name: Rebuild
'on':
  push:
    branches:
      - master
jobs:
  autofix:
    runs-on: ubuntu-latest
    if: github.actor != 'khan-actions-bot'
    steps:
      - uses: actions/checkout@v1
        name: '▶️ Setup checkout: get the repo'
      - run: yarn
        name: '▶️ Setup yarn: '
      - name: Rebuild our "dist" file
        run: yarn run dist
      - uses: Khan/autofix-commit-action@master
        env:
          GITHUB_TOKEN: '${{ secrets.KHAN_ACTIONS_BOT_TOKEN }}'
